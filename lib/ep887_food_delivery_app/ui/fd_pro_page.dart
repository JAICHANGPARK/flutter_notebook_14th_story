import 'package:flutter/material.dart';


class FdProPage extends StatelessWidget {
  const FdProPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pro Page'),
    );
  }
}
