import 'package:flutter/material.dart';


class FdGoOutPage extends StatelessWidget {
  const FdGoOutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Go out page'),
    );
  }
}
