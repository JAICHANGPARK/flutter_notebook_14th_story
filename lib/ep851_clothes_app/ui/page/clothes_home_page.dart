import 'package:flutter/material.dart';

class ClothesHomePage extends StatelessWidget {
  const ClothesHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
