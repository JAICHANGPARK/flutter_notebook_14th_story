
import 'package:flutter/material.dart';

class SalonBookingItem extends StatelessWidget {
  
  const SalonBookingItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
