import 'package:flutter/material.dart';

class MedicCareApp extends StatelessWidget {
  const MedicCareApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: MedicCareHomePage(),
    );
  }
}
